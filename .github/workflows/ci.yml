name: CI

  on:
    pull_request:
    push:
      branches:
        - "**"

  jobs:
    test:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - run: echo "intentional failure for Compi test"
        - run: exit 1